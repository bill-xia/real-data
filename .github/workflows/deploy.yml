name: Deploy Charts

on:
  push:
    branches: [ "main", "test" ]

permissions:
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Build site
        run: python generate_site.py

      - name: Copy csv files
        run: |
          cd data
          find . -name "*.csv"
          find . -name "*.csv" -exec cp --parents {} ../build/charts \;

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
